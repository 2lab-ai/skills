# llmux landing brief

## Thesis

**The harness is capital. Models are consumables.** The page makes the instability of model supply the opening tension, then proves that llmux keeps one Claude Code harness unchanged while routing its model names across independently managed backends.

## Audience and single job

For developers and agent operators who have accumulated Claude Code conventions, hooks, MCP servers, and subagents. The page's job is to make the architectural bet legible enough that they will install llmux and inspect it themselves.

## Proof object

**Routing ledger.** A CSS/HTML figure holds the harness constant on the left, routes five literal `/model` signals through a central llmux ledger, and resolves them into four backend groups on the right. Every lane remains readable as text without CSS or motion. It is not shared with the xfx example and is not derived from any reference-site DOM.

## Argument order

1. Thesis and real install command — state the bet and offer the reversible action.
2. Routing ledger — prove the core promise before listing capabilities.
3. Why the harness matters — explain what stays put.
4. Request receipts — show that routing is observable rather than magical.
5. Remote topology and Islands — extend the same daemon beyond one terminal.
6. Install/operate — repeat the copyable command and first actions.
7. Candor — state policy dependence, ownership boundary, and non-affiliation before the close.

The order moves from belief → mechanism → evidence → operating shape → boundary. It deliberately avoids a hero/features/pricing template.

## Tokens

- `--canvas: #f4f0e6` — warm paper.
- `--canvas-raised: #fbf8f1` — ledger sheets.
- `--ink: #1a1814` — dark brown-black.
- `--ink-muted: #625d52` — utility labels.
- `--rule: #cbc3b3` — hairlines.
- `--accent: #c3311d` — routing red, used only for the primary action, selected route, and live-state mark.
- `--accent-ink: #ffffff`.
- Computed `#1a1814` on `#f4f0e6`: approximately **15.2:1**, above the 12:1 system floor.

## Type roles

- Display: Iowan Old Style → Baskerville → Times New Roman. Job: thesis and editorial section statements.
- Body: Avenir Next → Avenir → system UI. Job: explanation and controls.
- Machine: SFMono-Regular → Consolas → Liberation Mono. Job: model names, requests, commands, and receipts.

No remote fonts or font requests. The hierarchy survives offline by construction.

## Motion

Seven reveal groups plus one route-state change over an approximately 5,700px page: about 1.4 animated elements per 1,000px, intentionally below the reference working range because this is an operator page. Tap feedback uses 150ms; artifact state change uses 300ms. Reduced-motion removes all reveal transforms and transitions.

## Claim ledger

| Page claim | Authoritative evidence |
|---|---|
| “Models change every month. Your harness shouldn't.” / harness-is-capital thesis | `/Users/zhugehyuk/2lab.ai/llmux/README.md:3`, `:13`, `:22` |
| Local Anthropic-compatible proxy for Claude Code; Claude Code talks to localhost:3456 | `README.md:13` |
| Subagents, slash commands, MCP servers, hooks, and CLAUDE.md conventions stay put | `README.md:13` |
| One Rust binary includes daemon, live TUI, login/import, updater, launcher | `README.md:15` |
| Four backend groups: Claude, Codex, Grok, OpenRouter | `README.md:16-20`, `:91-98` |
| Model-name routing signals and literal examples | `README.md:13`, `:79-98` |
| Multi-account quota-aware scheduling, cooldown parking, Fable ceilings | `README.md:17` |
| Live per-request receipts, raw four-leg viewer, copy-as-curl | `README.md:18`, `:113-116` |
| Remote-first central daemon with pure clients and per-machine keys | `README.md:19`, `:116-117` |
| Native macOS Islands companion and KDE/Qt port | `README.md:20`, `:36-40`, `:122-123` |
| Stable Homebrew install command | `README.md:24-28` |
| Preview and optional Islands install commands | `README.md:30-40` |
| Login/import commands and four browser login paths | `README.md:49-62` |
| `llmux run` starts/reuses daemon and launches Claude Code | `README.md:64-69` |
| Foreground dashboard and manual shell wiring | `README.md:71-77` |
| Stable/preview channel controls | `README.md:100-108` |
| One human using their own accounts; no pooling or resale | `README.md:125-130` |
| Third-party flat-rate subscription-token path is policy-dependent and opt-in | `README.md:129-131` |
| Not affiliated with Anthropic, OpenAI, xAI, or OpenRouter | `README.md:132` |

## Performance and accessibility receipt targets

- Standalone HTML; no build step, framework, external images, remote fonts, analytics, or widgets.
- Target uncompressed HTML ≤250KB, inline JS ≤5KB, zero external requests.
- Exactly one `h1`, ordered headings, one `main`, named sections, skip link, visible focus, 44px actions.
- Browser gate: 1440×1000 and 390×844, no body horizontal overflow; font-block test is vacuously stable because all stacks are local.
